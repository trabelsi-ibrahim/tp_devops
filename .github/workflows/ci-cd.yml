name: CI/CD Pipeline

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Build application
        run: |
          # Compile Java files
          javac -d . Calcul/*.java Clacul/*.java

      - name: Install dependencies
        run: |
          # Assuming there are no external dependencies to install

      - name: Run automated tests
        run: |
          # Run JUnit tests
          java -cp .:junit-platform-console-standalone.jar org.junit.platform.console.ConsoleLauncher --class-path . --scan-class-path

  deployment:
    name: Deployment
    runs-on: ubuntu-latest
    needs: [build] # This job depends on the 'build' job

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Deploy to production
        run: |
          # Commands to deploy your application to a production environment
